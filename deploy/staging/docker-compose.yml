services:
  staging_web:
    image: dima163sm/clicker-frontend:${IMAGE_TAG}
    container_name: staging_web
    restart: unless-stopped
    networks:
      - webnet

networks:
  webnet:
    external: true